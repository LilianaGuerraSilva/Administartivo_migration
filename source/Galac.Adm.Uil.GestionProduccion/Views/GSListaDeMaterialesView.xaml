<UserControl x:Class="Galac.Adm.Uil.GestionProduccion.Views.GSListaDeMaterialesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:nsWpf="clr-namespace:LibGalac.Aos.UI.Wpf;assembly=LibGalac.Aos.UI.Wpf"
             xmlns:nsWpfControls="clr-namespace:LibGalac.Aos.UI.WpfControls;assembly=LibGalac.Aos.UI.WpfControls"
             xmlns:nsVm="clr-namespace:Galac.Adm.Uil.GestionProduccion.ViewModel"
             xmlns:nsV="clr-namespace:Galac.Adm.Uil.GestionProduccion.Views">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/LibGalac.Aos.UI.WpfRD;component/rsdInputControl.xaml" />
                <ResourceDictionary Source="/LibGalac.Aos.UI.WpfRD;component/rsdButton.xaml" />
                <ResourceDictionary Source="/LibGalac.Aos.UI.WpfRD;component/rsdGroupBox.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="booleanToVisibilityConverter" />
            <DataTemplate x:Key="GroupBoxHeaderTemplate">
                <TextBlock Text="{Binding }"
                           FontWeight="Bold"
                           Foreground="Navy" />
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid FocusManager.FocusedElement="{Binding ElementName=txtCodigo}">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="400" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- aqui los groupbox -->
        <Label Grid.Row="0"
               Grid.Column="0"
               Name="lblFechaCreacion"
               Content="Fecha de Creación"
               Style="{StaticResource LabelPrompt}" />
        <nsWpfControls:GSDatePickerWpf Grid.Row="0"
                                       Grid.Column="1"
                                       Width="120"
                                       Style="{StaticResource DatePickerStyle}"
                                       Name="dtpFechaCreacion"
                                       SelectedDate="{Binding Path=FechaCreacion, NotifyOnValidationError=True, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                                       IsEnabled="False" />
        <Label Grid.Row="1"
               Grid.Column="0"
               Name="lblCodigo"
               Content="Código"
               Style="{StaticResource LabelPrompt}" />
        <nsWpfControls:GSTextBoxWpf Grid.Row="1"
                                    Grid.Column="1"
                                    Name="txtCodigo"
                                    nsWpf:LibFocusManager.IsFocused="True"
                                    nsWpf:LibFocusManager.FocusedPropertyName="Codigo"
                                    Style="{StaticResource TextBoxInputStyle}"
                                    InputDataType="Alfanumeric"
                                    MaxLength="30"
                                    Width="120"
                                    IsEnabled="{Binding Path=IsEnabledCodigo}"
                                    Text="{Binding Path=Codigo, NotifyOnValidationError=True, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                                    BorderThickness="1" />


        <Label Grid.Row="2"
               Grid.Column="0"
               Name="lblNombre"
               Content="Nombre"
               Style="{StaticResource LabelPrompt}" />

        <nsWpfControls:GSTextAreaWpf Grid.Row="2"
                                     Grid.Column="1"
                                     Name="txtNombre"
                                     Style="{StaticResource TextBoxInputStyle}"
                                     MaxLength="255"
                                     Height="45"
                                     nsWpf:LibKeyboardListener.IgnoreEnterKeyLikeTab="True"
                                     AcceptsReturn="True"
                                     IsReadOnly="{Binding Path=IsReadOnly}"
                                     Text="{Binding Path=Nombre, NotifyOnValidationError=True, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                                     Width="440"
                                     VerticalScrollBarVisibility="Auto"
                                     InvalidCharacters="&quot;|@°¬!#$%&amp;/()=?'¡¿¨;,:*+~&lt;&gt;^''{}" />


        <Label Grid.Row="3"
               Grid.Column="0"
               Name="lblCodigoArticuloInventario"
               Content="Código Artículo a Producir"
               Style="{StaticResource LabelPrompt}" />
        <nsWpfControls:GSSeekConnection Grid.Row="3"
                                        Grid.Column="1"
                                        Name="txtCodigoArticuloInventario"
                                        Height="23"
                                        Margin="3"
                                        HorizontalAlignment="Left"
                                        Width="280"
                                        IsEnabled="{Binding Path=IsEnabled}"
                                        DisplayMemberPath="Codigo"
                                        Model="{Binding Path=ConexionCodigoArticuloInventario, Mode=TwoWay}"
                                        ConnectionValue="{Binding Path=CodigoArticuloInventario, Mode=TwoWay, NotifyOnValidationError=True, ValidatesOnDataErrors=True}"
                                        SearchCommand="{Binding Path=ChooseCodigoArticuloInventarioCommand}" />
        <nsWpfControls:GSTextAreaWpf Grid.Row="4"
                                     Grid.Column="1"
                                     Name="txtDescripcionArticulo"
                                     Style="{StaticResource TextBoxInputStyle}"
                                     MaxLength="7000"
                                     Height="45"
                                     nsWpf:LibKeyboardListener.IgnoreEnterKeyLikeTab="True"
                                     AcceptsReturn="True"
                                     IsReadOnly="True"
                                     Text="{Binding Path=DescripcionArticuloInventario, NotifyOnValidationError=True, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                                     Width="440"
                                     VerticalScrollBarVisibility="Auto" />

        <!-- aqui llamado al detalle -->
        <Grid  Grid.Row="7"
               Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ScrollViewer  VerticalScrollBarVisibility="Auto"
                           Margin="3">
                <TabControl Grid.Row="0"
                            Grid.Column="0"
                            Grid.ColumnSpan="2"
                            Margin="3"
                            Name="tbcArticulos"
                            Width="600">
                    <TabItem Header="Artículos/Servicios"
                             Name="tbiSeccionListaDeMaterialesDetalleArticuloInventario">
                        <Grid Name="gtSeccionListaDeMaterialesDetalleArticuloInventario">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <ContentControl x:Name="dListaDeMaterialesDetalleArticulo"
                                            Grid.Row="0"
                                            Grid.ColumnSpan="2"
                                            Content="{Binding Path=DetailListaDeMaterialesDetalleArticulo.SelectedItem}"
                                            Focusable="False"
                                            IsEnabled="{Binding Path=IsEnabled}" />

                            <Border Height="25"
                                    Grid.Row="1"
                                    Grid.ColumnSpan="2"
                                    Background="Transparent"
                                    BorderBrush="LightGray"
                                    BorderThickness="0,0,0,1">
                                <StackPanel Orientation="Horizontal">
                                    <Button nsWpf:LibKeyboardListener.FocusTarget="{Binding ElementName=dListaDeMaterialesDetalleArticulo}"
                                            Command="{Binding Path=DetailListaDeMaterialesDetalleArticulo.CreateCommand}"
                                            ToolTip="Nuevo"
                                            Style="{StaticResource MenuButtonStyle}"
                                            IsEnabled="{Binding Path=IsEnabled}">
                                        <Image Source="/LibGalac.Aos.UI.WpfRD;component/Images/add.png"
                                               Focusable="False" />
                                    </Button>
                                    <Button Command="{Binding Path=DetailListaDeMaterialesDetalleArticulo.DeleteCommand}"
                                            ToolTip="Eliminar"
                                            Style="{StaticResource MenuButtonStyle}"
                                            IsEnabled="{Binding Path=IsEnabled}">
                                        <Image Source="/LibGalac.Aos.UI.WpfRD;component/Images/delete.png"
                                               Focusable="False" />
                                    </Button>
                                </StackPanel>
                            </Border>
                            <nsWpfControls:GSDataGrid Grid.Row="2"
                                                      Grid.ColumnSpan="2"
                                                      Style="{StaticResource GSDataGridStyle}"
                                                      MinHeight="150"
                                                      MinWidth="250"
                                                      Height="200"
                                                      Name="dgDetailListaMateriales"
                                                      KeyboardNavigation.IsTabStop="False"
                                                      IsSynchronizedWithCurrentItem="True"
                                                      Focusable="False"
                                                      IsReadOnly="True"
                                                      ItemsSource="{Binding Path=DetailListaDeMaterialesDetalleArticulo.Items}"
                                                      SelectedItem="{Binding Path=DetailListaDeMaterialesDetalleArticulo.SelectedItem, Mode=OneWayToSource}"
                                                      ColumnsMap="{Binding Path=DetailListaDeMaterialesDetalleArticulo.VisibleColumns}"
                                                      SelectedIndex="{Binding Path=DetailListaDeMaterialesDetalleArticulo.SelectedIndex}">
                            </nsWpfControls:GSDataGrid>
                        </Grid>
                    </TabItem>
                </TabControl>
            </ScrollViewer>
        </Grid >


    </Grid>




</UserControl>
